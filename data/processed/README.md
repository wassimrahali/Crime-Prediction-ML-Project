# Processed data

This folder contains generated train/val/test splits produced by the preprocessing pipeline:

- `train.parquet`, `val.parquet`, `test.parquet`

These artifacts are intentionally ignored by git because they are large and reproducible.
